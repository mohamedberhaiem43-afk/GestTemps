# robots.txt — Concorde Workforce
# Les zones applicatives privées ne sont pas explorées (gaspillage de budget crawl).
# Les pages non publiques (login, signup, verify-email, plan-configuration) restent
# explorables mais portent un en-tête HTTP « X-Robots-Tag: noindex, nofollow »
# (cf. nginx.conf) : Google les lit puis les exclut proprement de l'index.
# Les pages publiques (/, /download, /contact-sales) sont indexables.

User-agent: *
Disallow: /dashboard/
Disallow: /api/

Sitemap: https://concorde-work-force.com/sitemap.xml
